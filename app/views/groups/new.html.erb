<%= render 'shared/banner', name: "New Category", amount: nil%>
<% if @group.errors.any? %>
  <div class="alert alert-danger p-3">
    <h6><%= pluralize(@group.errors.count, "error") %> prohibited this post from being saved:</h6>

    <ul>
    <% @group.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<div class="card m-3">
  <div class="card-body">
    <%= form_with model: @group, url: groups_path, class: 'flex flex-col items-center w-6/12 mx-auto' do |form| %>
      <%= render 'groups/form', form: form%>
      <div class="actions">
        <%= form.submit "Submit", class: "btn btn-primary w-100 mt-2" %>
      </div>
    <%end%>
  </div>
</div>